import { useLocation, useNavigate } from 'react-router-dom'
import { getAuth, signInWithPopup, GoogleAuthProvider, F } from 'firebase/auth'

function OAuth() {
    return (
        <div>
            Google
        </div>
    )
}

export default OAuth
