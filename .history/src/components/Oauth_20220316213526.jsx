import { useLocation, useNavigate } from 'react-router-dom'
import { } from 'firebase/auth'

function OAuth() {
    return (
        <div>
            Google
        </div>
    )
}

export default OAuth
